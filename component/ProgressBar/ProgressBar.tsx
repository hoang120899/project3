export interface ProgressBarProps {}

export function ProgressBar(props: ProgressBarProps) {
  return (
    <div>
      {/* <Field
        validationMessage="Indeterminate ProgressBar"
        validationState="none"
      >
        <ProgressBar />
      </Field> */}
    </div>
  );
}
