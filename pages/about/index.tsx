import { Button } from "@fluentui/react-components";

export interface AboutProps {}

export default function About(props: AboutProps) {
  return (
    <div className="mt-10">
      <Button className="oalala">ahihi</Button>
    </div>
  );
}
